# WarpFactor — план разработки под GDD 0.1

Утверждён 2026-09-14. Заменяет прежний план этапов 5–8 из `ARCHITECTURE.md`.
Дизайн-документ: `docs/GDD.md`. Решения, принятые при обсуждении плана, — в разделе 6.

## 1. Что изменилось в целях

| Тема | Было | Стало |
|---|---|---|
| Жанр | Песочница без боя и проигрыша | Фабрика + выживание + оборона; Game Over по-прежнему нет |
| Структура игры | Кампания уровней с целями | Забег: постоянная база путешествует между одноразовыми планетами |
| Игрок | Свободная камера | Дрон: WASD, камера следует за ним, стройка в радиусе, может добывать руду |
| Хранение | Глобальный склад ядра | Ядра нет. Предметы лежат в складах и в инвентаре дрона (ячейки со стаками) |
| Стоимость построек | Списание ресурсов из ядра | Постройка — предмет: крафтится руками (очередь, как в Factorio), ставится из инвентаря |
| Прогресс | Пороги доставки и контракты уровня | Развитие базы: лаборатории (предметы + энергия), дерево исследований, расширения |
| Потеря | Нет | При телепорте теряется всё вне базы и вне площадки центрального шлюза |
| Давление | Нет | Угроза растёт со временем на планете, волны врагов |
| Бой | Запрещён | Прочность зданий, враги, турели, стены |
| Электричество | После всех этапов | Этап 11, до исследований (лабораториям нужна энергия) |
| Карты | Только ручные уровни + редактор | Генерация планет по типам |
| Песочница | Галочка при старте уровня | Творческий режим: постройки не расходуются, радиус не ограничен, угрозы нет |

## 2. Что остаётся из сделанного (этапы 1–4)

**Переносится без переписывания:**
- сетка, чанки, `BuildingManager`, размещение, поворот, выделение, копирование;
- тик симуляции, сон и пробуждение, ленты (SoA), логистика, буры, заводы, рецепты из данных;
- рендер: атласы, MultiMesh предметов, оверлеи, превью;
- формат `.fwmap`, меню, настройки, RU/EN, тесты.

**Переделывается:**
- `CoreStorage` и ядро уступают место `Inventory` у складов и у дрона;
- стоимость построек становится предметом-постройкой;
- `LevelDef` превращается в `PlanetDef`/`BaseDef`, `Game` — в забег с двумя мирами;
- камера следует за дроном.

**Отменяется:**
- цели уровней;
- разблокировки по порогам доставки;
- контракты на время;
- счётчик доставки в ядро и сжигание излишка.

## 3. Новая архитектура (крупными блоками)

**Предметы и постройки:**
- у каждой строящейся постройки есть свой `ItemType` (поле `building`);
- у `ItemType` есть размер стака;
- ручной рецепт постройки: ингредиенты из `BuildingDef.cost`, время `craft_time`, выход `craft_amount`;
- на лентах, в складах и инвентаре постройки — такие же предметы, как руда.

**Инвентарь (`Inventory`):**
- ячейки, в каждой один тип предмета до размера стака;
- кэш суммарного количества и свободного места делает проверку «влезет ли» O(1) — важно для складов, в которые грузят ленты.

**Дрон (`Drone`):**
- модель в тике симуляции с интерполяцией: позиция, инвентарь, радиус действий;
- добыча руды, очередь крафта (промежуточные детали докрафчиваются автоматически);
- все действия игрока в мире проверяют радиус.

**Забег (`Run`):**
- владеет миром базы, миром планеты, дроном, общими часами, угрозой, исследованиями и маршрутом;
- `Run.step()` прогоняет обе симуляции, передачу через шлюзы, угрозу и врагов в фиксированном порядке.

**Шлюзы — зеркальные пары:**
- центральный шлюз стоит в центре площадки на планете, его пара — в центре базы;
- предмет, вошедший в сторону шлюза, выходит с той же стороны пары; направление каждой стороны настраивается;
- дрон переходит между мирами, влетая в шлюз;
- переносной шлюз открывается исследованием, его пару игрок ставит в базе сам.

**Площадка центрального шлюза:**
- постоянная часть базы, переезжает при любой телепортации вместе с постройками и предметами;
- теряется только то, что уже разрушено;
- размер растёт исследованиями.

**Бой** — всё хранится как данные:
- `EnemySystem` (SoA, MultiMesh) и `FlowField` к шлюзу;
- `ThreatDirector` с кривой угрозы и волнами из `.tres`;
- турели на патронах через `Consume`.

**Сохранения:**
- бинарный файл из секций с версиями;
- каждая система пишет свою секцию.

**Цель по производительности:** 5000+ зданий суммарно и 1000 врагов при 60 FPS на ×1.

## 4. Этапы

Правила прежние:
- один этап за ответ, каждый — играбельная сборка;
- файлы на диск, в ответе список;
- после этапа — ручные проверки и ограничения;
- коммит на этап.

### Этап 5 — Дрон, инвентарь, склады, крафт (пока в одном мире) ✅
- Дрон: WASD, камера следует за ним, радиус стройки и взаимодействия, добыча руды с ЛКМ.
- Ядро и `CoreStorage` убираются. Контейнер и хранилище становятся складами с ячейками; разгрузчик берёт из складов.
- Постройки — предметы, стройка расходует их из инвентаря. Снос возвращает постройку и её содержимое.
- Ручной крафт: очередь со временем, автоматический докрафт промежуточных деталей, отмена с возвратом.
- Окно инвентаря и крафта. Окно постройки: содержимое склада или завода, перенос кликом. В меню стройки — количество в инвентаре и кнопка крафта.
- Творческий режим вместо песочницы.
- «Переливной/Обратный шлюз» переименовываются в «клапаны».
- **Проверка руками:** добыть руду дроном → скрафтить бур и ленты → бур → лента → склад → забрать из склада → скрафтить завод.

### Этап 6 — База и планета ✅
- `Run`: два мира тикают одновременно; `BaseDef` и `PlanetDef`.
- База 24×24, порта у шлюза два (вход в базу — запад, выход наружу — восток), вход дрона по F, площадка 15×15.
- Отдельный флаг «где можно ставить» не понадобился: в базе нет руды, буры туда не встают сами.
- Центральный шлюз и его пара в базе с зеркальными сторонами; дрон проходит через шлюз между мирами.
- Площадка вокруг шлюза на планете: граница, оверлей.
- `WorldView`: неактивный мир не рисуется, но симулируется.
- **Проверка руками:** добыча на планете → шлюз → пара в базе → завод в базе → склад базы.

### Этап 7 — Телепортация и забег ✅
- Зарядка телепорта у шлюза (30 с, отменяемая), экран итога «отправлено / переехало / потеряно».
- Переезд площадки: постройки с содержимым и настройками, площадка — металлическая платформа.
- Генерация планет по сиду и типу: руды, рельеф, место посадки. Точки появления врагов — на этапе 9.
- Звёздная карта-граф на 3 шага вперёд; типы планет: обычная и пустошь (безопасная, без ресурсов).
- Позже исследования будут постепенно открывать выбор планеты, просмотр её содержимого и дальних шагов.
- «Новый забег» в меню.
- **Проверка руками:** полный цикл «прилёт → добыча → телепорт → следующая планета», площадка переехала.

### Этап 8 — Сохранения ✅
- Бинарный файл: заголовок (название, планета, время игры, дата) и сжатое тело из секций с версией.
- Таблицы id предметов, зданий, полов и руд: сохранение переживает добавление нового контента.
- Сохраняется всё, чтобы игра продолжалась ровно с того же тика:
  - оба мира — рельеф, постройки с id, содержимым и настройками, предметы на лентах;
  - внутренности симуляции — очередь бодрствующих, планировщик, ожидающие;
  - дрон, инвентарь и очередь крафта;
  - звёздная карта, зарядка телепорта, очереди шлюза.
- Автосейв: по таймеру из настроек (2/5/10 мин), после прибытия на планету и перед телепортом.
- Быстрое сохранение и загрузка (F5 / F9).
- Слоты в меню паузы: сохранить, загрузить, удалить. В главном меню — «Продолжить» и «Загрузить».
- Тесты: сохранение → загрузка даёт те же байты; прогон после загрузки совпадает с непрерывным;
  перенос при изменении порядка предметов.

### Этап 9 — Угроза и враги ✅
- Прочность зданий из данных, урон, разрушение: без возврата, содержимое теряется. Полоски прочности.
- Угроза растёт со временем на планете по кривой из типа планеты; в пустоши её нет.
- Волны из данных (`ThreatDef`, `EnemyDef`), точки появления у краёв карты от генератора,
  предупреждение и отсчёт до волны.
- Враги — данные (SoA) и один MultiMesh: 2–3 наземных типа-плейсхолдера.
- Поле потоков к шлюзу; постройки на пути проходимы «с ценой» — враги их ломают.
- Дрона могут сбить: возрождение у центрального шлюза.
- Прорыв (шлюз уничтожен) — аварийная телепортация без зарядки на случайную соседнюю планету;
  площадка переезжает, разрушенное потеряно.
- Враги и угроза попадают в сохранение. Бенчмарк: 1000 врагов и 5000 зданий.

### Этап 10 — Оборона ✅
- Турели на патронах с ленты (патроны — обычные предметы через `ConsumeItems`), снаряды-данные,
  выбор цели.
- Стены двух уровней, ремонт построек.
- Оверлей радиусов, индикатор угрозы, таймер следующей волны, тревога при атаке на постройки.
- **Точка первого плейтеста:** весь цикл GDD §10 играбелен, настраиваем темп.

### Этап Д — Контент ранней игры, энергия, жидкости, исследования ✅
Дизайн-часть: ресурсы, рецепты и порядок открытия игрок расписал в Obsidian (`WarpFactor/Заметки.md`).
Реализованы столбцы **Early game** и **Early midgame**, всё прежнее содержимое удалено.
Сделано одним этапом (решение игрока), потому что контент без энергии и исследований не проверить.

- Ресурсы: гематит, камень, уголь (дрон и бур), малахит (только бур), вода (насос).
  Компоненты: слитки, кирпич, шестерня, медный кабель, резистор, научный набор. Таблицы — `docs/CONTENT.md`.
- Заводы: печь (рецепт по сырью, уголь) и сборщик (рецепт — настройка, электричество); научный цех.
- Энергия: опоры ЛЭП с проводами и зонами, термогенератор на угле, паровая цепочка (насос → трубы →
  бойлер → паровые генераторы). При нехватке мощности потребители замедляются.
- Исследования: 5 узлов, наборы сдаются вручную (медленно) или через научный цех (быстро);
  закрытые постройки и рецепты нельзя скрафтить, в меню они затемнены.
- Клапаны удалены, вместо них у маршрутизатора приоритетные вход и выход.
- Патроны: гильза (калибр) + наполнитель (обычный ресурс задаёт эффект: урон, поджог, осколки, скорострельность).
- Рабочее название игры — **WarpFactor** (переименование проекта, меню и документации).
- **Проверка руками:** добыть руду → печь на угле → научные наборы → «Добыча» → бур с опорой и генератором →
  малахит → медь → сборщик → патроны → турель; паровая станция у воды.

### Правки после этапа Д ✅
По списку игрока (`WarpFactor/Добавить.md` в Obsidian):
- дерево исследований с ветками и линиями связей;
- прямые провода; опоры протягиваются на наибольшем расстоянии (7 тайлов);
- протягивание ленты через препятствие ставит мостовой конвейер, через чужую ленту — перекрёсток,
  без них в инвентаре ничего не ломает;
- на воде можно ставить только трубы и насосы; подземные трубы (до 10 тайлов);
- окна всех зданий: отдельные ячейки сырья, топлива и продукта, полоски прогресса, питания, жидкостей, мощности;
- бур отдаёт добычу только с лицевой стороны;
- карта втрое больше по стороне, залежи и скалы крупнее и реже; камера не выходит за край карты.

### Этап 11 — Энергия, этажи базы, шлюз по исследованиям ✅
Объединён с частью бывших этапов 12–13 (решение игрока — одним этапом).
- Аккумулятор (своё исследование), окно сети с графиком спроса, выработки и заряда, оверлей сетей (P).
- Мобильная база — два этажа: площадка шлюза на планете (20×20, пять расширений по +4) и подземный этаж
  (бывшая база: открывается исследованием, 16×16, пять расширений по +6).
- Шлюз по исследованиям: передача предметов, порты I–II (до 3 входов и выходов), передача энергии, передача жидкостей.
- Лифты между площадкой и этажом: предметы в выбранном направлении, энергия, жидкости, дрон.
- Вторая ступень генерации отложена до контента мидгейма (этап 14).
- **Проверка руками:** исследовать «Подземный этаж» → F над шлюзом → «Передача предметов» → ленты через порты →
  «Передача энергии» → генератор на площадке питает сборщик на этаже → лифт → аккумулятор и окно опоры.

### Этап 12 — Дерево развития, угольный бур, ветка дрона ✅
- Новое дерево: «Электрика» → трубы и пар → аккумуляторы; «Электрика» → «Электробур» → промышленность,
  наука и логистика; «Оборона» от «Электрики».
- Угольный бур 1×1 на угле: доступен с начала, вчетверо медленнее электробура, на угле сам берёт себе топливо.
- Ветка улучшений дрона: скорость, добыча, прочность, автопушка, ремонт — по три ступени.
- Очередь исследований до 5 штук (ПКМ по карточке).
- Баланс: ручная добыча быстрее, ручная сдача наборов медленнее (12 с), контейнер 1×1, меньше прочности
  у центрального шлюза.
- **Проверка руками:** набрать наборы → «Электрика» → поставить «Электробур» и «Оборону» в очередь →
  угольный бур на угле кормит себя сам → ветка дрона заметно меняет полёт и добычу.

### Этап 13 — Удобство, творческий режим, чётный шлюз ✅
- Шлюз 2×2, после «Портов шлюза II» — 4×4; площадка симметрична вокруг его центра.
- Трубы: протягивание само ставит подземные через препятствия, ряд подземных — на наибольшем шаге.
- Просадка кадров от множества труб на экране: стыки и заполнение кэшируются по чанкам.
- Shift+ЛКМ — забрать продукцию завода, Shift+ПКМ — загрузить в него сырьё.
- Творческий режим: блоки-источники (предметы, энергия, жидкость) и поглотитель, вызов волн,
  «Полигон» с бесконечной стоимостью, кнопки «Исследовать заново» и «Открыть всё».
- График сети: ряд возможной выработки и кнопки выбора рядов.
- Водный бак, надпись «Камень ×11» при добыче, стрелки к врагам, зум дерева исследований,
  колесо над интерфейсом не меняет масштаб мира.
- **Проверка руками:** протянуть трубу через стену; заставить этаж трубами и посмотреть на FPS;
  Shift-клики по печи; в творческом забеге поставить источник и поглотитель, позвать волну.

### Этап 14 — Игроки и слой команд ✅
- Несколько игроков в забеге: свой дрон, инвентарь и очередь крафта; общие планета, постройки,
  исследования и улучшения дрона. Игроки могут быть на разных этажах.
- Все действия игрока идут командами и применяются в тике в строгом порядке (игрок, номер).
- Сохранение игроков, тест детерминизма: одинаковые команды — одинаковые забеги байт в байт.
- Проверка без сети: F7 добавляет игрока (в творческом режиме), F8 переключает.

### Этап 15 — Сеть (lockstep) ✅
- Транспорт за интерфейсом: ENet (локальная сеть и прямой IP), поиск игр рядом; Steam встанет
  на его место без правки игры.
- Хост и клиенты, вход по снимку забега, задержка ввода 4 тика, общий порядок команд.
- Контрольные суммы состояния, автопочинка снимком при расхождении.
- Дрон вышедшего остаётся стоять; вернувшийся под тем же именем занимает своё тело.
- Общие пауза и скорость времени.
- **Проверка руками:** у одного — меню паузы → «Открыть для сети»; у другого — меню → «Подключиться»
  → игра в списке локальной сети.

### Этап 16 — Steam ✅
- GodotSteam установлен как аддон; игра работает и без него.
- `SteamTransport` поверх того же lockstep, `SteamLobbies` — список игр, создание лобби, приглашения.
- App ID в настройках, по умолчанию 480 (тестовое приложение Valve).
- Известная проблема Steam-сборки Godot (старый steam_api64.dll) описана в `docs/STEAM.md`.
- **Проверка руками:** меню паузы → «Открыть через Steam»; у друга меню → «Подключиться» →
  игра в списке «Игры в Steam».

### Правки 16 — отзывчивость сетевой игры ✅
- Часы симуляции не списывали время на пропущенном тике: клиент отставал накопительно.
  Теперь `SimClock` получает `can_step` и прерывается, не теряя накопленного.
- Задержка ввода 4 → 2 тика (133 → 66 мс).
- Свой дрон и камера рисуются с упреждением на задержку ввода (только отрисовка).
- Оверлей Steam появляется только в игре, запущенной самим Steam. Кнопка «Пригласить друзей»
  теперь объясняет это и кладёт SteamID в буфер обмена как запасной путь.

### Правки 16б — часы клиента ✅
- Часы клиента стали следящей системой: запас подтверждённых тиков (`CLIENT_BUFFER`) и множитель
  времени (`time_scale`). Без запаса любая неровность сети была видна как рывок всего мира,
  а любая просадка кадров оборачивалась вечным отставанием — именно так игрок «застревал».
- Запас сам растёт на неровной связи и оседает на ровной; отставание больше 8 секунд чинится
  снимком по просьбе клиента (`RESYNC_REQUEST`, версия протокола 2).
- Вход в игру и починка чинят потерю команд тика снимка (`_send_full_state`).
- Упреждение своего дрона растёт и тает ровно со скоростью дрона — ушёл рывок в первый миг
  движения; у клиента упреждение учитывает и его запас.
- F3 показывает строку `net:` с запасом, темпом и задержкой ввода.

### Правки 16в — сцена клиента и настоящая сверка ✅
- Пересборка сцены после починки оставляла старые часы: мир клиента шагал дважды (а после двух
  починок — трижды). Отсюда тряска и «застревание».
- После снимка интерфейс клиента оставался привязан к дрону хоста: чужой инвентарь, чужой радиус,
  ничего не поставить.
- Сверка отпечатков почти никогда не выполнялась: клиент приходит в тик раньше, чем хост его
  исполнит, и у хоста не было своего отпечатка — сумма клиента молча отбрасывалась.
- Отпечаток стал массивом именованных частей: хост называет, что именно разошлось.
- Творческий забег терял волны при загрузке и в снимке — у клиента расходились враги.
- Новая проверка `tests/net_scene_check.tscn`: настоящая игровая сцена клиента, 25 секунд игры
  с волнами, сверка состояний и починка.

### Этап 12б — Продолжение исследований
- Следующие уровни научных наборов (их уже нельзя сдавать вручную) и лаборатория второго уровня.
- Эффекты: переносной шлюз и его пара на этаже; звёздная карта — выбор планеты, просмотр её содержимого,
  обзор дальних шагов; скорость шлюза.

### Этап 13 — Пространство базы
- Размеры этажей и лифты сделаны в этапе 11; дальше — форма этажей из данных, новые этажи.
- Компактные улучшенные версии зданий.
- Сборщики, которые крафтят постройки автоматически (меньше рутины на каждой планете).
- Инструмент переноса группы построек для перестройки базы.

### Этап 14 — Контент мидгейма
- Midgame и late game из заметок: сфалерит, нефть, боксит, киноварь, аутунит и их цепочки.
- Титановые (быстрые) версии логистики возвращаются, артиллерия возвращается в данные вместе с крупным калибром.
- Новые типы планет и врагов, баланс угрозы и темпа. Сила волн от числа построек игрока
  (решение из обсуждения этапа 9) — формула и что считается постройкой.

### Этап 15 — Планеты
- Полноценный генератор по типам: биомы, рельеф, особые объекты.
- Аномалии и безопасные зоны.

### Этап 16 — Эндгейм и полировка
- Условие победы и конечная цель.
- Строительство со временем (возможно, дронами).
- Спрайты из Aseprite, звук и музыка, UX.
- GDExtension для горячих систем, если попросят бенчмарки.

## 5. Риски

1. **Две симуляции одновременно.** Держим сон и пробуждение, бенчмарк «база 3000 + планета 3000».
2. **Поле потоков на GDScript.** Пересчёт кусками на несколько тиков, иерархия по чанкам; запасной план — GDExtension.
3. **Враги и снаряды.** SoA и пространственная сетка; при нехватке — GDExtension.
4. **Рост сохранений.** Секции с версиями, тест детерминизма.
5. **Много TBD в GDD.** Системы делаем на данных, контент остаётся плейсхолдером до этапа Д.

## 6. Принятые решения (обсуждение 2026-09-14)

| Вопрос | Решение |
|---|---|
| Материалы для стройки | Постройки крафтятся из предметов в инвентаре дрона и ставятся из инвентаря |
| Хранение | Ядра нет; склады и инвентарь дрона с ограниченным числом ячеек |
| Инвентарь | Ячейки со стаками, как в Factorio |
| Ручной крафт | Очередь со временем, промежуточное докрафчивается само; переработка руды — только в заводах |
| Игрок | Дрон (WASD), стройка в радиусе; враги могут сбить, возрождение у шлюза |
| Добыча дроном | Да, медленно |
| Шлюзы | Центральный фиксированный (вход дрона в базу) и переносной (открывается исследованием); зеркальные пары |
| Где строить | Везде. За пределами площадки центрального шлюза всё теряется при телепорте |
| Прорыв | Площадка переезжает, как при обычной телепортации; теряется только разрушенное |
| Планеты | Генерация по типам |
| Исследования | Лаборатория — отдельная постройка, тратит предметы и электричество |
| Строительство | Пока мгновенное, со временем — в будущем |
| Клапаны | Были переименованы из «шлюзов» в «клапаны», на этапе Д удалены совсем: их роль выполняют приоритеты маршрутизатора |
| Сбитый дрон (этап 9) | Инвентарь и сырьё очереди крафта выпадают грузом на месте гибели; подобрать — подлететь. Вне площадки при телепорте груз теряется |
| Цели врагов (этап 9) | Идут к центральному шлюзу по полю потоков, по дороге бьют постройки и дрона в радиусе, ломают перегородившие путь |
| Рост угрозы (этап 9) | Сначала волны; затишья между ними сокращаются, и со временем всё превращается в одну непрерывную усиливающуюся волну |
| Аварийный телепорт (этап 9) | На случайную соседнюю планету звёздной карты |
| Проходимость (этап 9) | Ленты и логистика проходимы для врагов; заводы, буры, склады — твёрдые |
| Шлюз после телепорта (этап 9) | Пересобирается с полной прочностью |
| Ремонт (этап 10) | Дрон чинит бесплатно, только временем: сам, ближайшую повреждённую постройку в радиусе строительства |
| Оружие дрона (этап 10) | Слабая автопушка без патронов: бьёт ближайшего врага в радиусе — шанс отбиться, даже если ресурсов нет |
| Турели (этап 10) | Пулемёт (медь, графит, кремний) и артиллерия (графит, кремний; взрыв, мёртвая зона) |
| Патроны (этап 10) | С ленты и руками через окно турели; забрать обратно нельзя |
| Название (этап Д) | Рабочее название игры — WarpFactor |
| Объём этапа Д | Контент, энергия и исследования — одним этапом |
| Добыча (этап Д) | Дрон добывает всё раннее сырьё (гематит, камень, уголь); малахит и дальше — только буром |
| Исследования (этап Д) | Наборы первого уровня можно сдавать вручную из инвентаря, но намного медленнее; научный цех автоматизирует. Следующие уровни наборов — только цехом |
| Патроны (этап Д) | Гильза (постоянный компонент, зависит от калибра) + наполнитель (любой обычный ресурс задаёт эффект). Патрон собирается заранее — руками или в сборщике |
| Жидкости (этап Д) | Трубы и насосы (не «бутылки в контейнерах») |
| Турели (этап Д) | Без электричества |
| Старый контент (этап Д) | Удалён полностью; код артиллерии и уровней логистики оставлен для будущих этапов |
| Размер карты (правки Д) | ×3 по стороне: обычная планета 336–480 × 252–360 тайлов |
| Появление врагов (правки Д) | Как и раньше, у краёв карты — на большой карте волна идёт до шлюза дольше |
| Дерево исследований (правки Д) | Добыча → Логистика → Промышленность → Автоматизация науки; Добыча → Оборона |
| Бур и разгрузчик (правки Д) | Бур сам отдаёт только с лицевой стороны, разгрузчик забирает с любой |
| Ток в базе (этап 11) | Через шлюз, но после исследования «Передача энергии»; жидкости — «Передача жидкостей» |
| Аккумулятор (этап 11) | Добавлен сейчас, со своим исследованием |
| Вторая ступень генерации (этап 11) | Отложена до мидгейма |
| Состояние сети (этап 11) | Окно сети с графиком и оверлей всех сетей |
| Этажи (этап 11) | Верхний этаж — площадка шлюза на планете (20×20, +4 за шаг, 5 шагов); нижний — бывшая база, открывается исследованием (16×16, +6 за шаг, 5 шагов) |
| Связь этажей (этап 11) | Сначала только шлюз (дрон, порты лент); лифты — исследованием |
| Порты шлюза (этап 11) | Передача предметов — исследованием; ещё порты — +1 вход и выход за шаг, до трёх |
| Объём этапа 11 | Одним этапом |
| Сила волн | От глубины звёздной карты не зависит: у игрока, потерявшего всё, должен быть шанс развиться заново. **Позже:** сила волн зависит от числа построек игрока в базе и на планете — чем больше промышленности, тем быстрее развиваются враги; сами по себе враги не усиливаются. Пока — рост от номера волны и времени на планете |
